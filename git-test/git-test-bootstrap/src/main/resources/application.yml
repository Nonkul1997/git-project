server:
  # 端口
  port: 31000
  servlet:
    # 上下文
    context-path: /git-test

spring:
  # 当前环境
  profiles:
    active: dev
  # 数据源
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.108.59.231:3306/mydatabase
    username: root
    password: 123456
  # jackson时间格式化
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis-plus:
  # 实体类设置别名
  type-aliases-package: org.example.project.po
  global-config:
    # 主键生成策略
    db-config:
      id-type: auto

logging:
  # 打印sql语句
  level:
    org.example.project.mapper: trace


