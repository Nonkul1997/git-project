# 端口和上下文
server:
  port: 31000
  servlet:
    context-path: /test

spring:
  # 数据源
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://47.108.59.231:3306/mydatabase?serverTimezone=UTC
    username: root
    password: 123456
  # jackson时间格式化
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss

# 打印sql语句
logging:
  level:
    org.example.project.mapper: trace
