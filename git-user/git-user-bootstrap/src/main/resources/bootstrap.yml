spring:
  # 项目名称
  application:
    name: git-user
  # 当前环境
  profiles:
    active: dev
  # nacos默认监听${spring.application.name}和${spring.application.name}.${file-extension}
  # 和${spring.application.name}-${spring.profiles.active}.${file-extension}
  cloud:
    nacos:
      config:
        enabled: false
        server-addr: 47.106.64.47:8848
        namespace: c9dea2e8-8172-4ccf-b3fc-282a59acf04b
        group: git-user
        file-extension: yml
